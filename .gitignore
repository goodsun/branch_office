# Credentials — NEVER commit these
config/.anthropic_key
config/*_credentials*
config/*_token*
config/*_secret*
config/*.key
*.env
.env*

# Personal data (deployed to ~/.openclaw/workspace/, never committed there)
# template_workspace/ is the source template (git-managed)

# Temp zones (disposable by design)
assets/tmp/*
!assets/tmp/.gitkeep
documents/drafts/*
!documents/drafts/.gitkeep

# OS junk
.DS_Store
Thumbs.db

# Node
node_modules/

# Server-local (each division manages its own)
projects/*
!projects/.gitkeep
scripts/*
!scripts/.gitkeep
!scripts/common/
documents/*
!documents/.gitkeep
!documents/company_rules/
